# Dashboard
## 1. Setup Python
```
py -3.10 -m venv venv
.\venv\Scripts\activate
# pip install --upgrade pip
(venv) python -m pip install --upgrade pip
```

##  2. Install Packages
```
pip install bleak rich asyncio
```

# WebDash
## 1. Setup Python
```
py -3.10 -m venv venv
.\venv\Scripts\activate
# pip install --upgrade pip
(venv) python -m pip install --upgrade pip
```
##  2. Install Packages
```
pip install fastapi uvicorn[standard] bleak
```


